# 🚀 Step 1 — Add Docker to Your PATH (Permanent Fix)

echo 'export PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH"' >> ~/.zshrc

# Then reload your shell:

source ~/.zshrc

# ✅ Step 2 — Test It

# Now run:

docker --version


# You should see something like:

Docker version 24.x.x

# 🔎 Step 3 — Confirm Location

# Run:

which docker


# It should return:

/Applications/Docker.app/Contents/Resources/bin/docker

# 🔥 Now Build Your Image

# Go back to your project folder and run:

docker build -t rahul-cypress-custom .

# ✅ Then Run It

# Since your Dockerfile has:

CMD ["npm", "run", "test:smoke"]


# Just run:

docker run --rm rahul-cypress-custom


# It will automatically execute your smoke tests.

# 🔁 Run Other Test Types

# Override the default command:

# Regression chunk 1
docker run --rm rahul-cypress-custom npm run test:regression:chunk1

# Firefox
docker run --rm rahul-cypress-custom npm run test:smoke:firefox

# Retest
docker run --rm rahul-cypress-custom npm run test:retest


# Junit Merger 
npx junit-merge -d /Users/ilesanmi/Desktop/T.A/Automation_checkOne-/cypress/results -o /Users/ilesanmi/Desktop/T.A/Automation_checkOne-/cypress/results/results.xml

# mocawesome merge
npx mochawesome-merge "cypress/results/mochawesome/*.json" > mochawesome.json && npx marge mochawesome.json